&НаСервере
Процедура УдалитьНаСервере()
	Код = ЭтаФорма.Объект.Код;
	Обработка.отправитьСообщение(Код, "Вы удалены из базы.");
	УдалЭлемент = Справочники.Пользователи.НайтиПоКоду(Код);
	УдалЭлемент.ПолучитьОбъект().Удалить();
КонецПроцедуры

&НаКлиенте
Процедура Удалить(Команда)
	УдалитьНаСервере();
КонецПроцедуры

&НаСервере
Процедура ДоступНаСервере()
	Код = ЭтаФорма.Объект.Код;
	ПроверитьЭлемент = Справочники.Пользователи.НайтиПоКоду(Код).ПолучитьОбъект();
	ДоступБулево = ПроверитьЭлемент.ЕстьДоступ;
	Если ДоступБулево = Истина Тогда
		//Сообщить("ТеперьЛожь");
		ПроверитьЭлемент.ЕстьДоступ = Ложь;
		ЭтаФорма.Элементы.ФормаКоманднаяПанель.ПодчиненныеЭлементы.ФормаДоступ.ЦветФона = (webЦвета.красный);
		ПроверитьЭлемент.Записать();
	Иначе 
	    ДоступБулево = Ложь;
		//Сообщить("ТеперьИстинна");
		ПроверитьЭлемент.ЕстьДоступ = Истина;
		ЭтаФорма.Элементы.ФормаКоманднаяПанель.ПодчиненныеЭлементы.ФормаДоступ.ЦветФона = (webЦвета.Зеленый);
		ПроверитьЭлемент.Записать();
	КонецЕсли;
КонецПроцедуры

&НаКлиенте
Процедура Доступ(Команда)
	ДоступНаСервере();
КонецПроцедуры
