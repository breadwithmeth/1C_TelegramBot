&НаСервере
Процедура ОтправитьВсемНаСервере()
	ЭтаФорма.Отправлено.Очистить();
	Текст = ЭтаФорма.Объект.Текст;
	Выборка = Справочники.Пользователи.Выбрать();
	Пока Выборка.Следующий() = 1 Цикл
		Подписчик = Выборка.Код;
		Имя = Выборка.Наименование;
		Если Выборка.ЕстьДоступ = Истина Тогда
			Обработка.отправитьСообщение(Подписчик, Текст);
			ЭтаФорма.Отправлено.Добавить(Имя + " - " + Подписчик);
		//Сообщить(Имя + " " + Строка(Подписчик));
		КонецЕсли;
	КонецЦикла;
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьВсем(Команда)
	ОтправитьВсемНаСервере();
КонецПроцедуры







&НаСервере
Процедура ОтправитьДляОдногоНаСервере()
	// Ничего	
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьДляОдного(Команда)
	ОткрытьФорму("Обработка.Телеграм.Форма.ФормаОтправкиМоно");
	//ОтправитьДляОдногоНаСервере();
КонецПроцедуры
