
&НаСервере
Процедура ОтправитьПользователюНаСервере()
	Подписчик = ЭтаФорма.Объект.Пользователь.Код;
	Текст = ЭтаФорма.Объект.Текст;
	Сообщить(Строка(Подписчик) +"  "+ Текст);
	Обработка.отправитьСообщение(Подписчик, Текст);
КонецПроцедуры

&НаКлиенте
Процедура ОтправитьПользователю(Команда)
	ОтправитьПользователюНаСервере();
КонецПроцедуры

&НаСервере
Процедура ПользовательПриИзмененииНаСервере()
	ЭтаФорма.Айди = ЭтаФорма.Объект.Пользователь.Код;
КонецПроцедуры

&НаКлиенте
Процедура ПользовательПриИзменении(Элемент)
	ПользовательПриИзмененииНаСервере();
КонецПроцедуры
